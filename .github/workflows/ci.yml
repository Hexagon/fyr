name: Quick Checks

on:
  push:
    branches:
      - "**"

permissions:
  contents: read

concurrency:
  group: quick-checks-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: ./.github/workflows/validation-reusable.yml
